# Frontend Implementation Rules

适用范围：`apps/frontend/` 下全部小程序实现与页面契约。

## 视觉基调

- 页面统一追求：简约、柔和、有呼吸感、克制但不冷淡。
- 高级感来自秩序、留白、文字层级和轻描边，不来自装饰堆砌。
- 不做花哨运营风、彩虹功能面板、厚重卡片、强渐变堆叠。

## 场景色调规则

- 品牌内容页：
  - 首页
  - 社区
  - 我的
  - AI 诊断
  - 统一走紫白系、轻粉感、柔和品牌氛围
- 交易确认页：
  - 商城
  - 商品详情
  - 购物车
  - 结算
  - 支付相关页面
  - 地址管理
  - 地址编辑
  - 统一走暖灰白、米白、低饱和中性色
- 绿色只用于成功状态，不扩散成整页主色。

## 核心流程

- 页面实现统一遵循：`.pen -> schema -> code -> screenshot verification`。
- 所有前端契约与实现都必须以 `.pen` 的最新版本为准；如果 `.pen` 已更新，schema 和代码必须跟着更新，绝对不允许继续沿用旧 `.pen` 内容，更不允许用旧内容去覆盖新的 `.pen` 状态。
- 若目标页 `.pen` 仍在调整，或用户尚未明确确认“页面已经可以落实到代码”，前端必须停在画布修改阶段，禁止提前写 WXML/WXSS/JS。
- 前端页面契约唯一目录：`apps/frontend/docs/design-schemas/`。
- 未进入 schema 的页面，不应直接凭印象写 WXML/WXSS/JS。
- schema 生成与刷新必须通过 Pencil 官方 MCP / pencli 对应工具直接读取 `.pen` 节点结构完成，禁止用视觉识别、截图理解或人工看图方式替代。

## 实现约束

- WXML 结构顺序必须服从 schema `sections`。
- WXSS 优先服从 schema `tokens` 与布局规则。
- JS 只负责把 API payload 转换成页面状态，不应擅自扩展视觉块。
- 如果代码需要新增视觉模块，先改 schema，再改代码。
- 未确认前，不得自行新增、删除、重排模块。
- 若 `.pen` 已定义弹层、转场、支付验证、地图交互、成功反馈，前端不得退化实现。

## 前端视觉与交互落地规则

- 卡片要轻，不要厚；优先白底、轻描边、轻阴影。
- 强调色要少而准；默认以柔和紫色为主强调色。
- 顶部结构做减法，一个页面只保留一个强焦点。
- 操作区做轻反馈化处理，不做控制面板感。
- 允许克制型微交互，例如轻缩放、轻回弹、轻透明度过渡。
- 不允许大面积粒子、持续悬浮动画、多处同时高存在感动效。

## 小程序专属规则

- 默认使用微信原生导航栏。
- 禁止伪造系统状态栏时间、信号、电量。
- 未确认前，不得启用 `navigationStyle: custom` 重做导航。

## 当前实现口径

- 地址链路只认：
  - `address-list.page-schema.json`
  - `address-edit.page-schema.json`
- `pet-profile.page-schema.json` 已存在，但 `pages/pet/profile/index` 仍待实现承接。

## 验收要求

- 没有截图对照，不得宣称 1:1 还原。
- 截图只承担验收职责，不承担 schema 主生成职责。
- 视觉断言优先于“看起来差不多”。
